function deleteSaveBtn() {
    //선수 작업
        // 카카오 접속 확인
        // 로딩 부분.
        // 버튼 비활성.

    // 현재 모든 폼 nodeList로 가져와서 > 비활성 카드는(반드시 안에 데이터들어 있다고 보장할 수 있음) > /delete 호출하면 됨(페이로드는 [{}, {}, {}]

}